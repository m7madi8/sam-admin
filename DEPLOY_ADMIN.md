# رفع لوحة التحكم على الاستضافة

## لماذا تظهر رسالة "لم يُحمّل ملف admin-auth.js"؟

ملف **admin-auth.js** (الذي يحتوي اسم المستخدم وهاش كلمة السر) **مستثنى من Git** لأسباب أمان، لذلك عند رفع المشروع من المستودع إلى الاستضافة **لا يُرفع هذا الملف** ويبقى غير موجود على السيرفر.

- **محلياً (Live Server):** الملف موجود عندك في `assets/js/admin-auth.js` فكل شيء يعمل.
- **على الاستضافة:** الملف غير موجود في الملفات المرفوعة، فيفشل تحميله وتظهر الرسالة.

---

## الحل: إنشاء الملف على السيرفر بعد كل نشر

بعد كل مرة ترفع فيها المشروع (Git push أو رفع ملفات)، أنشئ ملف **admin-auth.js** يدوياً على الاستضافة:

### الخطوات

1. **على جهازك:** افتح الملف  
   `assets/js/admin-auth.js`  
   وانسخ **كل** محتواه (من أول سطر إلى آخر سطر).

2. **على الاستضافة:**
   - ادخل إلى لوحة التحكم (cPanel أو غيره).
   - افتح **مدير الملفات** (File Manager) أو استخدم **FTP**.
   - انتقل إلى مجلد موقعك ثم إلى المسار:  
     `assets/js/`
   - أنشئ ملفاً جديداً باسم: **admin-auth.js**
   - الصق المحتوى الذي نسخته من جهازك واحفظ الملف.

3. تأكد أن المسار النهائي على السيرفر هو:  
   `.../assets/js/admin-auth.js`  
   (نفس المسار المستخدم في الصفحة).

4. حدّث صفحة لوحة التحكم في المتصفح وجرّب تسجيل الدخول (مثلاً: `https://موقعك.com/f1.html`).

---

## مثال لمحتوى admin-auth.js

يجب أن يبدو الملف بهذا الشكل (مع قيمك أنت):

```javascript
window.ADMIN_AUTH = {
  username: 'samar',
  passwordHash: '1862916000'
};
```

- **username:** اسم المستخدم الذي تريده للدخول.
- **passwordHash:** ناتج دالة الهاش لكلمة السر (انظر `admin-auth.example.js` لطريقة التوليد).

لا ترفع هذا الملف إلى Git؛ أنشئه فقط على السيرفر بعد النشر.
